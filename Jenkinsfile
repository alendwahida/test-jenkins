node('Node-Docker') {
    stages {
        stage('build app') {
            steps {
                sh 'ls -la'
            }
        }
        stage ('test app') {
            steps {
                sh 'du -sh *'
            }
        }
    }
}
