node('agent1') {
    stages {
        stage('build app') {
            steps {
                sh 'ls -la'
            }
        }
        stage ('test app') {
            steps {
                sh 'du -sh *'
            }
        }
    }
}
